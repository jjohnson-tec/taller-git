[project]
name = "taller"
version = "0.1.0"
description = "Proyecto ejemplo para Taller Git"
requires-python = ">=3.8"

[tool.poetry]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"